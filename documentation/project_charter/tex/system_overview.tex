Explain, at a high level, how you will implement a solution to the problem. Include a diagram of major components to the system (not a full architectural design, but a high level overview of the major system components and how a user or external system might interface). Avoid specific implementation details (operating system, programming languages, etc.). This section should occupy at least 1 full page.

\usepackage{graphicx}

We will design a SIEM with human readability/simple system configuration and component understanding. Only applying at most a handful of security components to give the user a simple SIEM management for easy access.
System Overview:
    Environment: PSIEM will live entirely and be entirely accessible through a cloud Environment with Reverse Proxy framework 
    Core Components: With the environment set, our dashboard will be composed of React UI, Login and authenticaiton system, and AI assitant chatbox, providing the user a display mechanism for better readability, usability, and visual sense
    Sub Components:

\includegraphics{images/SystemDiagram}